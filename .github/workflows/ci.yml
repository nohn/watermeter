name: CI

on:
  push:
    branches: [ main, v1.4 ]
  pull_request:

jobs:
  docker-build:
    runs-on: ubuntu-latest
    steps:
      - name: Pull Code
        uses: actions/checkout@v3
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      - name: Docker buildx (no push)
        if: github.event_name == 'pull_request'
        uses: docker/build-push-action@v7
        with:
          context: .
          platforms: linux/amd64,linux/arm64,linux/arm/v7,linux/arm/v8,linux/arm/v9
          push: false
      - name: Docker build
        run: docker build . --target test -t nohn/watermeter
      - name: Run Smoke Test
        run: docker run nohn/watermeter:latest php /usr/src/watermeter/public/index.php | grep 1189.3858
      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v4
        with:
          images: |
            nohn/watermeter
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
      - name: Login to DockerHub
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Push to Docker Hub (latest)
        if: github.ref == 'refs/heads/v1.4' && github.repository == 'nohn/watermeter' && github.event_name != 'pull_request'
        uses: docker/build-push-action@v7
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          pull: true
          push: true
          cache-from: type=registry,ref=nohn/watermeter:latest
          cache-to: type=inline
          tags: nohn/watermeter:latest
          labels: ${{ steps.meta.outputs.labels }}
          no-cache: true
      - name: Push to Docker Hub (develop)
        if: github.ref == 'refs/heads/main' && github.repository == 'nohn/watermeter' && github.event_name != 'pull_request'
        uses: docker/build-push-action@v7
        with:
          context: .
          platforms: linux/amd64,linux/arm64,linux/arm/v7,linux/arm/v8,linux/arm/v9
          pull: true
          push: true
          cache-from: type=registry,ref=nohn/watermeter:develop
          cache-to: type=inline
          tags: nohn/watermeter:develop
          labels: ${{ steps.meta.outputs.labels }}
          no-cache: true
